# StreamDecoder
**StreamDecoder** is a low-latency pipeline that captures implicit digital body language (hovering, text highlights, erratic scrolling) to detect on-page consumer friction. Running events through an in-memory cache against a synthetic intent matrix, it triggers immediate micro-interventions (custom chatbots or rewards) before session drop-off.
